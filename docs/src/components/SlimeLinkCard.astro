---
import { LinkCard } from '@astrojs/starlight/components';

interface Props {
  title: string;
  href: string;
  description?: string;
  external?: boolean;
  rel?: string;
}

const { title, href, description, external = false, rel } = Astro.props;
---

<div
  class="slime-link-card"
  class:list={[
    'h-full',
    '[&_.sl-link-card]:rounded-lg [&_.sl-link-card]:border-border [&_.sl-link-card]:bg-card',
    '[&_.sl-link-card]:h-full',
    '[&_.sl-link-card]:transition-[background-color,border-color,box-shadow] [&_.sl-link-card]:duration-200 [&_.sl-link-card]:ease-in-out',
    '[&_.sl-link-card:hover]:border-primary [&_.sl-link-card:hover]:bg-accent [&_.sl-link-card:hover]:shadow-md',
    '[&_.sl-link-card_.title]:text-card-foreground [&_.sl-link-card_.description]:text-muted-foreground [&_.sl-link-card_.icon]:text-primary',
  ]}
>
  <LinkCard
    title={title}
    href={href}
    description={description}
    target={external ? '_blank' : undefined}
    rel={rel ?? (external ? 'noreferrer' : undefined)}
  />
</div>

<style>
  .slime-link-card :global(.sl-link-card) {
    align-items: start;
  }

  .slime-link-card :global(.sl-link-card:hover),
  .slime-link-card :global(.sl-link-card:focus-within) {
    border-color: var(--slime-primary);
  }

  .slime-link-card :global(.sl-link-card .title) {
    text-decoration-line: underline;
    text-decoration-style: dashed;
    text-decoration-color: color-mix(in oklch, var(--slime-primary) 60%, transparent);
    text-decoration-thickness: 2px;
    text-underline-offset: 0.18em;
    transition: text-decoration-color 200ms ease-in-out;
  }

  .slime-link-card :global(.sl-link-card:hover .title),
  .slime-link-card :global(.sl-link-card:focus-within .title) {
    text-decoration-style: solid;
    text-decoration-color: var(--slime-primary);
  }

  .slime-link-card :global(.sl-link-card .icon) {
    margin-block-start: 0.15rem;
    color: var(--slime-primary);
  }

  .slime-link-card :global(.sl-link-card:has(a[rel='prev']) .icon) {
    transform: rotate(180deg);
  }

  .slime-link-card :global(.sl-link-card:hover .icon),
  .slime-link-card :global(.sl-link-card:focus-within .icon) {
    color: var(--slime-primary);
  }
</style>
